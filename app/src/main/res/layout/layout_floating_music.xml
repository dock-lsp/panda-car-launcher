<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="320dp"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_floating_card"
    android:orientation="vertical"
    android:padding="12dp"
    android:elevation="8dp">

    <!-- 顶部栏：歌曲信息 + 缩放 + 关闭 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="8dp">

        <!-- 缩放按钮 -->
        <ImageView
            android:id="@+id/floating_music_zoom_in"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:src="@drawable/ic_plus"
            android:padding="4dp"
            android:background="?android:attr/selectableItemBackgroundBorderless" />

        <ImageView
            android:id="@+id/floating_music_zoom_out"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_marginStart="4dp"
            android:src="@drawable/ic_minus"
            android:padding="4dp"
            android:background="?android:attr/selectableItemBackgroundBorderless" />

        <View
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <!-- 关闭按钮 -->
        <ImageView
            android:id="@+id/floating_music_close"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:src="@drawable/ic_close"
            android:padding="4dp"
            android:background="?android:attr/selectableItemBackgroundBorderless" />

    </LinearLayout>

    <!-- 歌曲信息 -->
    <TextView
        android:id="@+id/floating_music_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="未在播放"
        android:textColor="@color/text_primary"
        android:textSize="18sp"
        android:textStyle="bold"
        android:singleLine="true"
        android:ellipsize="end" />

    <TextView
        android:id="@+id/floating_music_artist"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:text=""
        android:textColor="@color/text_secondary"
        android:textSize="14sp"
        android:singleLine="true"
        android:ellipsize="end" />

    <!-- 播放控制 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:paddingTop="12dp"
        android:paddingBottom="4dp">

        <!-- 上一首 -->
        <ImageView
            android:id="@+id/floating_music_prev"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:src="@drawable/ic_skip_prev"
            android:padding="8dp"
            android:background="@drawable/bg_control_btn" />

        <!-- 播放/暂停 -->
        <ImageView
            android:id="@+id/floating_music_play"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="16dp"
            android:src="@drawable/ic_play"
            android:background="@drawable/bg_icon_cyan"
            android:padding="12dp" />

        <!-- 下一首 -->
        <ImageView
            android:id="@+id/floating_music_next"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:src="@drawable/ic_skip_next"
            android:padding="8dp"
            android:background="@drawable/bg_control_btn" />

    </LinearLayout>

</LinearLayout>
